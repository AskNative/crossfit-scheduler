.GridContainer
  // center()
  padding 0

.GridCol
  col()
  // center(max_width = 400px, pad = 0)
  // align(both)
  // background whitesmoke
  margin 20px 0
  padding-bottom 40px
  border-bottom 1px solid whitesmoke

  +above(3)
    col(1/2, cycle: 2)
    border-bottom none

  // +tablet()
  //   center(500px)

  +desktop()
    col(1/3, cycle: 3)
